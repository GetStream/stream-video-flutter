import 'dart:ui' as ui;

/// Captures the frame the web video element of the renderer with [textureId]
/// shows. Always null off the web, where the frame is read from the texture.
Future<ui.Image?> captureElementFrame(int? textureId) async => null;
