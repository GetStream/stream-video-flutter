name: stream_video_noise_cancellation_example
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  stream_video: ^1.2.3
  stream_video_noise_cancellation: ^1.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

dependency_overrides:
  stream_video:
    path: ../../stream_video
  stream_video_noise_cancellation:
    path: ../../stream_video_noise_cancellation

flutter:
  uses-material-design: true
