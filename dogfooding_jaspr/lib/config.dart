/// Matches the Flutter dogfooding app's default environment so both clients
/// share the same apiKey/token source and can join the same call.
/// See `dogfooding/lib/core/model/environment.dart`.
const kEnvironmentName = 'pronto';
const kTokenHost = 'pronto.getstream.io';
